# HTML Tag Teams Assignment

## This project is now in the style of the Franciscan Port

Miguel selected the colors from their website and styled the CSS in this project to match it. 

## Team 3

Tyler Dean

Miguel Lock

Mia Giswold

## Bootstrap project

Initially we had just installed bootstrap via the bootstrap.min.css file. For the team assignment, since bootstrap had to be compiled and variables overridden, we installed bootstrap using 'npm install bootstrap'.

Then I edited the _variables.scss

Then I created a SCSS folder and added a file called 'custom.scss' where I overrode bootstrap's default styling. 